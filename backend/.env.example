# backend/.env.example
#
# This file is intentionally minimal to avoid duplication.
# See the root-level .env.example file for all available configuration options.
#
# The backend application will read environment variables from:
# 1. Root .env file (if running via Docker Compose)
# 2. backend/.env.test file (for testing)
#
# To configure your environment:
# - Copy ../.env.example to ../.env and configure for your setup
# - For testing: Copy .env.test.example to .env.test or use the existing .env.test
